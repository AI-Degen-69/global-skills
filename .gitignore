# Hermes runtime state — managed by the agent, not user content.
# Keep these out of version control so commits don't churn on agent activity.
.hub/
.curator_state
.usage.json
.usage.json.lock

# OS noise
.DS_Store
Thumbs.db

# Python caches
__pycache__/
*.pyc

# Curator backup archives (too large for GitHub)
.curator_backups/
